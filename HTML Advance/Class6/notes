frontend naming:
1. javascript(JS)
backend nameing:
2. node.JS

if the terminal cannot find the folder do (ls) to see what the computer have access to then use (cd) to lead to the folder you want